{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the Google Chat window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
